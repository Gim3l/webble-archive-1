# Migration `20201002202645`

This migration has been generated by Gimel Dick at 10/2/2020, 4:26:45 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "ResourceFolder" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL,
    "classroomId" INTEGER NOT NULL,
    "resourceFolderId" INTEGER,

    FOREIGN KEY ("classroomId") REFERENCES "Classroom"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("resourceFolderId") REFERENCES "ResourceFolder"("id") ON DELETE SET NULL ON UPDATE CASCADE
)
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201001021331..20201002202645
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -62,8 +62,9 @@
   updatedAt                 DateTime                    @updatedAt
   TeacherProfileOnClassroom TeacherProfileOnClassroom[]
   StudentProfileOnClassroom StudentProfileOnClassroom[]
   GradeOnStudentProfile     GradeOnStudentProfile[]
+  ResourceFolder            ResourceFolder[]
 }
 model TeacherProfile {
   id                        Int                         @id @default(autoincrement())
@@ -140,4 +141,14 @@
   assignmentId Int
   @@id([gradeId, assignmentId])
 }
+
+model ResourceFolder {
+  id               Int              @id @default(autoincrement())
+  name             String
+  classroom        Classroom        @relation(fields: [classroomId], references: [id])
+  classroomId      Int
+  folders          ResourceFolder[] @relation("ResourceFolderToResourceFolder")
+  ResourceFolder   ResourceFolder?  @relation("ResourceFolderToResourceFolder", fields: [resourceFolderId], references: [id])
+  resourceFolderId Int?
+}
```


